# Copy this file to `.env` and fill in your own Alpaca paper trading
# credentials (never commit the real `.env` — it's already gitignored).
# Get paper trading keys from https://app.alpaca.markets/paper/dashboard/overview

APCA_API_KEY_ID=your-paper-key-id
APCA_API_SECRET_KEY=your-paper-secret-key
APCA_API_BASE_URL=https://paper-api.alpaca.markets

# Postgres connection string for trade telemetry logging
# (src/utils/db.py). Required for src.trading.alpaca_execution to record
# executed trades; the rest of the app works fine without it.
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Upstash Redis REST credentials (src/utils/cache.py), used to cache slow
# / rate-limited yfinance calls (financial statements, historical
# prices). Optional — if unset, caching is simply disabled and every
# call hits yfinance directly. Get these from your database's page at
# https://console.upstash.com/
UPSTASH_REDIS_REST_URL=https://your-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token

# Service-to-service bearer token required by src/api/main.py's
# /api/evaluate/{ticker} endpoint. Only the Next.js server (via its
# VALUATION_API_TOKEN in frontend/.env.local) should ever know this
# value — generate with e.g. `openssl rand -hex 32`. The two copies
# (this one and the frontend's) must match exactly. Validated identically
# at first use on both sides (this backend's VALUATION_API_TOKEN_MIN_LENGTH
# in src/api/main.py, the frontend's VALUATION_API_TOKEN_REQUIREMENT in
# frontend/src/lib/secret-validation.ts) — format/placeholder/length
# checks only, not an entropy measurement: must be set; not whitespace-
# only or whitespace-padded; not the placeholder value shown below; not
# a single character repeated; and at least 32 characters — otherwise
# every request is refused (503).
VALUATION_API_TOKEN=replace-with-a-long-random-value
